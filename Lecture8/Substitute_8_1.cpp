#include <string>
#include <iostream>
#include <iterator>
#include <vector>

template < class in, class x >
in find(in b, in e, const x& x ) {
  while(b!=e&&*b!=x) {
    ++ b;}
    retrun b;
  }
int main(int argc, char const *argv[]) {
  find
  return 0;
}
